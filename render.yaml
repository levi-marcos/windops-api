services:
  - type: web
    name: windops-api
    runtime: node
    repo: https://github.com/REPLACE_WITH_OWNER/REPLACE_WITH_REPO
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npm run start:prod
    envVars:
      - key: NODE_VERSION
        value: 24